apiVersion: v1
kind: Service
metadata:
  name: agent2web
  namespace: agent2web
  labels:
    app.kubernetes.io/name: agent2web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent2web
  ports:
    - name: http
      port: 80
      targetPort: http
