apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: agent2web

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  # Create secret.yaml from secret.example.yaml first, then uncomment:
  # - secret.yaml

images:
  - name: ghcr.io/raveli/agent2web
    newTag: latest
